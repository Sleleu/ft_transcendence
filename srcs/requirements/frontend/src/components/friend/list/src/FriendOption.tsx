import React from 'react'
import '../css/FriendOption.css'
import { User } from '../../../types'

type propsOption = {
    token: string
    friend: User
}

const FriendOption = ({ token, friend }: propsOption) => {
    return (
        <div>
        </div>
    )
}

export default FriendOption